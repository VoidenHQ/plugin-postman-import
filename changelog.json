[
  {
    "version": "v1.0.6",
    "date": "08/07/2026",
    "title": "Import Crash, Path Params & Description Fixes",
    "changes": {
      "Improved": [
        "Postman-file detection now only scans the first 64KB of a .json tab's content instead of the entire buffer, so the check for the \"Import as Postman\" action stays fast regardless of file size instead of rescanning a multi-hundred-KB collection on every render"
      ],
      "Fixed": [
        "Importing any Postman collection or environment export no longer throws \"isPostmanEnvironmentExport is not defined\" — the button component was calling the check without importing it",
        "Path-param extraction no longer mistakes a literal URL port for a path parameter — \"http://localhost:3000/api/:id\" previously mangled the URL to \"http://localhost{3000}/api/{id}\" instead of just \"{id}\"",
        "Request-level descriptions are now inserted as markdown documentation right after the title heading, instead of being concatenated onto the very end of the file with no separation (and sometimes glued onto the last block's closing fence)",
        "Request-level and collection-level descriptions now correctly handle Postman's \"{ content, type }\" description object shape, not just plain strings",
        "The collection-level root documentation file (from the collection's own description) is now written as a well-formed .void file with proper frontmatter and a title heading, instead of raw text with no frontmatter that the editor didn't recognize as a valid document",
        "Large request bodies (multi-hundred KB to MB, common for base64-encoded file payloads) no longer risk freezing the app during import — writes now go through the same 512KB chunked-write path the rest of the app already uses for large saves",
        "Importing a Postman collection no longer drops the description set on headers, path parameters, and query parameters — they're now carried into the Description column of the corresponding table instead of being silently discarded"
      ]
    }
  },
  {
    "version": "v1.0.5",
    "date": "03/07/2026",
    "title": "GraphQL Import Fix & Performance",
    "changes": {
      "Fixed": [
        "GraphQL requests now import with the correct gqlquery container format (gqlurl + gqlbody children) required by the voiden-graphql plugin — previously generated a flat format that caused a blank query block",
        "CRLF line endings in imported GraphQL query bodies are now normalized to LF, preventing garbled single-line output in the editor",
        "GraphQL requests no longer also generate a redundant request block — the URL is placed inside the gqlurl child node instead"
      ],
      "Improved": [
        "Environment/globals export detection now uses a fast string probe instead of JSON.parse, eliminating unnecessary parsing of large collection files on every render"
      ]
    }
  },
  {
    "version": "v1.0.4",
    "date": "24/06/2026",
    "title": "Auth, Scripts, File Uploads & GraphQL",
    "changes": {
      "Added": [
        "Auth: basic/bearer/apikey/oauth2/oauth1/digest/ntlm/awsv4/hawk are now converted into a native auth block instead of only basic/bearer/apikey being flattened into headers",
        "Pre-request and test scripts are imported as commented-out pre_script/post_script blocks (Postman's pm.* API isn't translated automatically since Voiden scripts use a different voiden.*/vd.* API — review and adapt before enabling)",
        "Multipart form-data file fields (type: file) are imported as fileLink attachments when the file still exists at its original path on this machine, instead of being silently dropped",
        "GraphQL request bodies (body.mode: graphql) are converted into gqlquery/gqlvariables blocks instead of being skipped"
      ],
      "Changed": [
        "Completion state no longer leaves the import button stuck green — it now shows a check icon with a brief \"Completed\"-style message for 2 seconds (plus a toast notification) before resetting to idle"
      ]
    }
  },
  {
    "version": "v1.0.3",
    "date": "24/06/2026",
    "title": "Path Params & Environment Import",
    "changes": {
      "Added": [
        "Colon-style path segments (e.g. :id) in request URLs are converted to brace-style placeholders (e.g. {id})",
        "Path parameters are extracted from Postman's url.variable list (or inferred from :name segments when absent) and imported as a path-table block with their values",
        "Postman environment and globals exports can now be imported directly — enabled variables are written into the project's .env files, using the environment name as the comment header"
      ]
    }
  },
  {
    "version": "v1.0.2",
    "date": "01/04/2026",
    "title": "Initial Release",
    "description": "Import Postman Collection v2.0 and v2.1 files into Voiden.",
    "changes": {
      "Added": [
        "Postman Collection v2.0 and v2.1 import",
        "Folder structure preserved as Voiden documents",
        "Environment variable mapping",
        "Auth inheritance from parent folders"
      ]
    }
  }
]
